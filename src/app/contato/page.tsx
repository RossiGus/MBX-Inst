export default function ContatoPage() {
  return (
    <section className="wrap min-h-[60vh] pb-[var(--sec-y)] pt-[clamp(112px,15vh,168px)]">
      <span className="font-mono text-[11px] font-medium uppercase tracking-[0.16em] text-brass">
        Contato
      </span>
      <h1 className="mt-[14px] text-[clamp(27px,4.6vw,52px)] font-bold leading-none tracking-[-0.03em]">
        Vamos desenhar a sua cozinha.
      </h1>
      <p className="mt-[16px] max-w-[46ch] text-ink-2">
        {/* Fase 1: portar o formulário .req do original (submit → WhatsApp). */}
        Formulário de contato em construção — Fase 1.
      </p>
    </section>
  );
}
